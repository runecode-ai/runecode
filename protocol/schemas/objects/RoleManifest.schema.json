{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://runecode.dev/protocol/schemas/objects/RoleManifest.schema.json",
  "title": "RoleManifest",
  "description": "Protocol-owned role manifest family. Security-sensitive capability expansion must be explicit, signed, versioned, and bound to a shared principal identity.",
  "type": "object",
  "additionalProperties": false,
  "maxProperties": 8,
  "required": [
    "schema_id",
    "schema_version",
    "principal",
    "role_kind",
    "approval_profile",
    "capability_opt_ins",
    "allowlist_refs",
    "signatures"
  ],
  "properties": {
    "schema_id": {
      "const": "runecode.protocol.v0.RoleManifest",
      "description": "Stable protocol object-family identifier for role manifests.",
      "x-data-class": "public"
    },
    "schema_version": {
      "const": "0.2.0",
      "description": "Exact protocol schema version used to validate this role manifest.",
      "x-data-class": "public"
    },
    "principal": {
      "$ref": "PrincipalIdentity.schema.json",
      "description": "Shared principal identity for the role or daemon this manifest governs so later approvals and audit events bind to the same identity object.",
      "x-data-class": "sensitive"
    },
    "role_kind": {
      "type": "string",
      "minLength": 1,
      "maxLength": 64,
      "description": "Named role category this manifest configures, such as gateway or workspace.",
      "x-data-class": "public"
    },
    "approval_profile": {
      "type": "string",
      "enum": [
        "moderate"
      ],
      "description": "Approval profile selected as a signed manifest input for this role. MVP currently allows only the moderate profile.",
      "x-data-class": "public"
    },
    "capability_opt_ins": {
      "type": "array",
      "maxItems": 64,
      "description": "Explicit capability opt-ins enabled by this signed role manifest. An empty array is the intentional signed no-capabilities posture.",
      "x-data-class": "public",
      "items": {
        "type": "string",
        "minLength": 1,
        "maxLength": 128,
        "description": "Stable capability identifier explicitly enabled by the manifest."
      },
      "uniqueItems": true
    },
    "allowlist_refs": {
      "type": "array",
      "maxItems": 64,
      "description": "Digest references to allowlist artifacts bound into the manifest signature input.",
      "x-data-class": "public",
      "items": {
        "$ref": "Digest.schema.json#/$defs/digestValue",
        "description": "Digest reference to an allowlist artifact consumed by this manifest."
      },
      "uniqueItems": true
    },
    "signatures": {
      "type": "array",
      "minItems": 1,
      "maxItems": 8,
      "description": "Detached signatures attesting to the exact manifest contents.",
      "x-data-class": "public",
      "items": {
        "$ref": "SignedObjectEnvelope.schema.json#/$defs/signatureBlock",
        "description": "Detached signature block for the manifest payload."
      }
    }
  }
}
